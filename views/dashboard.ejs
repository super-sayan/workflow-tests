<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/skeleton/2.0.4/skeleton.css"
    />
    <link href="/css/my_styles.css" rel="stylesheet"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta http-equiv="X-UA-Compatible" content="ie=edge" />
    <title>Dashboard</title>
    <!--appointment table-->
    <script type="text/javascript" src="https://github.com/rubyeffect/easy_fill/tree/master/lib/easy_fill-0.0.2.min.js"></script>
  </head>
  <body>
    <!-- <h1>Dashboard</h1> -->

    <a href="/users/logout">Logout</a>
    <ul>
      <% if (typeof errors != 'undefined') { %> <% errors.forEach(error => { %>
      <li><%= error.message %></li>
      <% }) %> <% } %>
    </ul>

    <ul>
      <% if(messages.success_msg) { %>
      <li><%= messages.success_msg %></li>
      <% } %>
    </ul>

    <ul>
      <% if (messages.error) { %>
      <li><%= messages.error %></li>
      <% } %>
    </ul>
    <div id="container">
      <div id="body_header">
        <!--This is a division tag for body header-->
        <h1>Appointment Request Form</h1>
        <h3>Hello <%= user %></h3>
        <h4>Make your appointments more easier</h4>
  
      </div>
      <form action="/users/dashboard" method="post">
        <fieldset>
          <h5>Your basic details:</h5>
          <label for="name">Name*:</label>
          <input type="text" id="name" name="user_name" value=<%= user %> required pattern="[a-zA-Z0-9]+">
  
          <label for="mail">Email*:</label>
          <input type="email" id="email" name="user_email" value=<%= email %> required>  
  
        </fieldset>
  
        <fieldset>
          <label for="meal_kind">Appointment for*:</label>
          <select id="meal_kind" name="meal_kind" required>
            <option value="breakfast">Breakfast</option>
            <option value="lunch">Business Lunch</option>
            <option value="dinner">Dinner</option>
          </select>
          <label for="appointment_remark">Appointment Remark:</label>
          <textarea id="appointment_remark" name="appointment_remark"></textarea>
          <label for="date">Date*:</label>
          <input type="date" name="date" value="" required></input>
          <br>
          <label for="time">Time*:</label>
          <input type="time" id="time" name="time" min="09:00" max="18:00" required />
          <small>Cafe is open from 9am until 6pm.</small>
          <br>
          <!-- <label for="duration">For How Long?(Minutes)</label>
          <input type="radio" name="duration" value="30" checked> 30
          <input type="radio" name="duration" value="60"> 60
          <input type="radio" name="duration" value="90"> 90
          <input type="radio" name="duration" value="more"> more -->
        </fieldset>
        <input type="submit" value="Request for appointment" />
      </form>
    </div>
  </body>
</html>
